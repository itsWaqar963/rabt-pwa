NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Web Push (optional). Browser push subscription needs this public key;
# a server must send Web Push with the matching private key for true
# background delivery when the app is killed / backgrounded.
# NEXT_PUBLIC_VAPID_PUBLIC_KEY=
